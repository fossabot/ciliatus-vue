export default {
    api: {
        basePath: window.location.hostname + '/',
        prefix: 'api/v1/',
        xDebug: true
    },
    web: {
        basePath: window.location.hostname + '/',
        prefix: 'web/'
    }
}
